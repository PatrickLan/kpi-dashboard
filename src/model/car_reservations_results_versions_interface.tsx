export interface CarReservationResultsVersions
{
    modified: string | null,
    state: string | null,
    reservation_start: string | null,
    reservation_end: string | null,
    note: string | null,
    purpose: number | null,
    lift_offer: boolean
}